package ${packageName};

import com.github.rogerli.framework.dao.Mapper;
import ${typeName};
import org.springframework.stereotype.Repository;

@Repository
public interface ${className} extends Mapper<${entityName}, String> {

}
